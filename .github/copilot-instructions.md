# Copilot Instructions

## Project Guidelines
- User prefers Dashboard data endpoints inside MVC DashboardController rather than separate API controllers.
- User prefers responses in Arabic.
- Default shift start time configured to 08:35 local time for attendance checks.
- Match employee by email only (do not fallback to name) when checking attendance.